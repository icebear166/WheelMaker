# Personal Wiki 知识规则

- 默认只查询 Git `HEAD` 中的已提交知识。
- 没有当前对话中针对具体候选的明确批准时，不得修改、提交或发布 Wiki。
- 文章归属只在 `content/registry/articles.yaml` 中维护；不要按分类移动 Markdown 文件。
- 原始对话、草稿、临时发现和任何凭据不得进入仓库。
- 发布只使用 Personal Wiki Kit 的知识白名单命令，不使用 `git add -A`。
