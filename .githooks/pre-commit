#!/bin/sh
set -eu

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required; install it with: go install github.com/zricethezav/gitleaks/v8@v8.28.0" >&2
  exit 1
fi

gitleaks git --staged --redact --no-banner
